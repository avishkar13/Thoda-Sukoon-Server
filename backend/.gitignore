# This file tells Git which files and folders to ignore in your project.

# Dependencies
# This folder contains all the project dependencies installed by npm/yarn.
# It can be very large and is always re-created by running 'npm install'.
/node_modules

# Environment Variables
# NEVER commit this file. It contains secrets like database passwords and API keys.
# Instead, create a '.env.example' file with placeholder values to show other
# developers what variables are needed.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
# Log files are generated during runtime and are specific to an environment.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build Output
# If you are using TypeScript or another build tool, ignore the output directory.
/dist
/build

# Test Reports
# Test coverage reports are generated artifacts and shouldn't be in version control.
/coverage

# IDE & Editor Directories
# These are user-specific settings for code editors.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS-generated files
# System files created by macOS, Windows, or Linux.
.DS_Store
Thumbs.db
ehthumbs.db

# Optional: PM2 process files
# If you use PM2 for process management, you can ignore its files.
*.pid
*.pid.lock